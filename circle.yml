checkout:
  post:
    - git submodule sync
    - git submodule update --init
    - mvn package
    - cp -R ./target $CIRCLE_ARTIFACT

machine:
  java:
    version: oraclejdk8

general:
  artifacts:
    - "nukkit-1.0-SNAPSHOT.jar"
    - "nukkit-1.0-SNAPSHOT-shaded.jar"
