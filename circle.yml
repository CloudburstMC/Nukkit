checkout:
  post:
    - git submodule sync
    - git submodule update --init
    - mvn clean package
    - cp -R ./server/target/nukkit-1.0-SNAPSHOT.jar $CIRCLE_ARTIFACTS

machine:
  java:
    version: oraclejdk8
